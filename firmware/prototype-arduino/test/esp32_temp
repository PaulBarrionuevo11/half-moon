// #pragma once

// #include "driver/temperature_sensor.h"

// class ESP32

// {
//     public:
//     ESP32();
//     void get_mcu_temperature() 
//         {
//         temperature_sensor_handle_t temp_handle = NULL;
//         temperature_sensor_config_t temp_sensor = TEMPERATURE_SENSOR_CONFIG_DEFAULT(10, 80); // Set range
//         ESP_ERROR_CHECK(temperature_sensor_install(&temp_sensor, &temp_handle));
        
//         // Start sensor
//         ESP_ERROR_CHECK(temperature_sensor_start(temp_handle));
        
//         ESP_ERROR_CHECK(temperature_sensor_get_celsius(temp_handle, &sensor_temperature));
//         printf("Temperature: %f °C\n", sensor_temperature);
        
//         // Clean up
//         ESP_ERROR_CHECK(temperature_sensor_stop(temp_handle));
//         ESP_ERROR_CHECK(temperature_sensor_uninstall(temp_handle));
//     }
//     private:
//         float sensor_temperature;

// };